{
  "devDependencies": {
    "lint-staged": "^15.2.0",
    "husky": "^8.0.0"
  },
  "lint-staged": {
    "*.{js,svelte,vue,ts}": "npx --yes oxlint@latest --deny-warnings"
  },
  "scripts": {
    "prepare": "husky install",
    "lint": "npx --yes oxlint@latest --deny-warnings"
  }
}
